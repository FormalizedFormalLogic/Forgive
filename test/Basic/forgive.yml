version: v0

accept:
  - propext
  - Quot.sound
  - Classical.choice

declaration:
  TestLib.add_comm':
    forgive:
      - TestLib.add_comm'
  TestLib.uses_add_comm':
    forgive:
      - TestLib.add_comm'
